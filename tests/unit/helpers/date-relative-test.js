import {
  dateRelative
} from 'getting-started-with-ember/helpers/date-relative';

module('DateRelativeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateRelative(42);
  ok(result);
});
